name: Reusable Deploy
on: workflow_call
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Get artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist-files
      - name: Output content
        run: ls
      - name: Deploy
        run: echo "Deploying..."